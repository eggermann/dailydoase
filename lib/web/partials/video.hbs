<video id="video{{json.id}}" class="responsive-video" data-mtime="{{metadata.mtime}}" controls loading="lazy" autoplay muted playsinline loop>
    <source src="/v{{src}}" type="video/mp4">
    Your browser does not support the video tag.
</video>
{{log .}}